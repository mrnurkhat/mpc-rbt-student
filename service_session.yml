name: "communication_example"
root: <%= @settings["working_directory"]%>

startup_window: monitoring
windows:
  - monitoring:
      layout: even-vertical
      panes:
        - watch -n 1 "systemctl status example_sender"
        - watch -n 1 "systemctl status example_receiver"
